# ============================================================
# PRIVATE — These files and folders are NEVER committed
# ============================================================

# Internal planning — local only
private/
MASTERPLAN.md
*.private.md
*.internal.md

# Environment and credentials — NEVER
.env
.env.*
.env.local
*.key
*.secret
secrets/

# Local Cloudflare Pages / wrangler dev secrets — NEVER commit the real file.
# Use .dev.vars.example as the committed template instead.
.dev.vars
.dev.vars.*
!.dev.vars.example

# Dev and admin tools — local only
studio.html
dev.html
admin.html

# Dependencies + local QA tooling — NEVER tracked (this was the "3K+" untracked noise)
node_modules/
.qa/
*.log

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo

.wrangler
.dev.vars
